<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent" >

    <com.gionee.client.view.widget.PullToRefreshListView
        xmlns:ptr="http://schemas.android.com/apk/res-auto"
        android:id="@+id/message_list"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@drawable/messges_list_divider"
        android:cacheColorHint="@color/transparent"
        android:divider="@drawable/messges_list_divider"
        android:dividerHeight="0.5dp"
        android:fadingEdge="none"
        android:listSelector="@android:color/transparent"
        android:overScrollMode="never"
        android:scrollbars="none"
        android:stretchMode="columnWidth"
        ptr:ptrOverScroll="false" />

    <ViewStub
        android:id="@+id/no_message_data"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@id/cut_title"
        android:layout="@layout/no_date_layout" />

</RelativeLayout>